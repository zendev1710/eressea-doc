# Development

## REquirements

## Building the web site

In a PowerShell console, from the folder containing the `docs` folder:  `mkdocs build`

## Running in local mode

In a PowerShell console, from the folder containing the `docs` folder:

- To run the web site in local: `mkdocs serve &`
- To stop the web site: `Stop-Process -Name python`
